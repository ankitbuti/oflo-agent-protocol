# ── Core async / web ──────────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
aiohttp>=3.9.0
pydantic>=2.6.0
python-dotenv>=1.0.0

# ── LLM providers ─────────────────────────────────────────────────────────────
anthropic>=0.37.0          # Claude (prompt caching, tool use, streaming)
openai>=1.30.0             # GPT-4o, o3, embeddings
google-generativeai>=0.5.0 # Gemini 2.x (optional)

# ── OpenAI Agent SDK (Swarm successor) ────────────────────────────────────────
openai-agents>=0.0.7       # optional — remove if not using OpenAI Agent SDK

# ── LangChain / LangGraph ─────────────────────────────────────────────────────
langchain>=0.2.0
langchain-core>=0.2.0
langchain-anthropic>=0.2.0
langchain-openai>=0.1.0
langchain-google-genai>=1.0.0  # optional
langchain-groq>=0.1.0           # optional
langgraph>=0.1.0

# ── Vector memory (optional — Weaviate v4 client) ─────────────────────────────
weaviate-client>=4.5.0     # optional — remove if not using Weaviate

# ── Dev / testing ─────────────────────────────────────────────────────────────
pytest>=8.0.0
pytest-asyncio>=0.23.0
black>=24.0.0
isort>=5.13.0

# ── Observability (optional) ──────────────────────────────────────────────────
# aimon>=0.5.0             # uncomment to re-enable AIMon guardrails
